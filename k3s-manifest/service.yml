apiVersion: v1
kind: Service
metadata:
  name: swiggy-app
  namespace: poojitha
  labels:
    app: swiggy-app
spec:
  selector:
    app: swiggy-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
  type: LoadBalancer

  
